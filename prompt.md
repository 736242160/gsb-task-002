我想用 Python 做一个零外部依赖的轻量级有向无环图（DAG）任务流水线调度与回滚引擎，并在本地提供一个可通过浏览器查看的实时状态看板。全栈代码只允许使用 Python 官方标准库（如 http.server、threading、json、unittest 等），前端仅使用纯原生 HTML/CSS/JS（严禁使用第三方包、外部库或 CDN）。
引擎需要支持声明式定义任务的依赖关系与执行动作，并根据拓扑依赖进行多线程并发调度。调度前必须严格检测并拦截循环依赖。任务调度受限于最大并发线程数限制，无依赖分支需并行执行，只有前序所有依赖全部成功后才可唤醒下游。关键逻辑在于故障级联回滚：当某个节点发生异常且重试耗尽时，引擎需立刻中止后续未运行任务，并严格按照依赖关系的逆序（逆拓扑序），对所有已成功执行的前置节点触发补偿回滚，确保多并发分支下的回滚一致性与数据状态安全，杜绝死锁与竞态。
服务需内置一个轻量本地 HTTP 监控看板。前端采用简单的分层节点卡片与状态列表展示（无需复杂连线计算），清晰反映每个节点在等待、运行中、成功、重试、回滚中、已回滚或失败的状态变色。页面提供一键触发演示功能，预置正常多分支汇聚流程与中途失败触发倒序回滚流程各一条，点击后可通过定时轮询实时查看节点状态跃迁与操作日志输出。
请在项目目录下编写好完备的自动化单测（使用标准库 unittest），重点覆盖环依赖检测、并发限流顺序以及节点故障后的逆拓扑补偿回滚逻辑，保证直接运行 python -m unittest discover 即可全部测试通过。同时提供一个开箱即用的主入口，运行后既能执行测试，又能启动本地服务在浏览器中查看状态看板。
